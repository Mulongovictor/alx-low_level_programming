#include "main.h"
#include <stdio.h>

/**
 * add - sums u to numbers.
 * @a: is the first number.
 * @b: is the second number.
 *
 * Return: sum of the two numbers.
*/

int add(int a, int b)
{
	int sum;

	sum = a + b;

	return (sum);
}
