#include <stdlib.h>
#include <stdio.h>

/**
 * main - the function that prints the file name.
 * Return: (0) Always SUCCESS
 */
int main(void)
{
	printf("The file compiled is %s\n", __FILE__);
	return (0);
}
